server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/discodeit
    username: user1
    password: user1password
  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.sprint.mission.discodeit: info
    org.hibernate.SQL: info

management:
  endpoint:
    health:
      show-details: never
  info:
    env:
      enabled: false