개선점 채널 생성-> 채널 생성시 채널 만든 사람은 자동으로 참여해야하는거 아님?
save-> save시 temp로 만들어서 저장하지말고 원래 기존 파일을 임시 파일로 저장하고
이후 직접 파일에 저장하다가 실패 시 모두 임시파일로 rollback하는 방식이 더 좋지 않나?
(트랜잭션, 원자성을 지키는 방식으로)

-> 채널의 참가된 수를 미리 저장해둘까?, 메시지 수나?
-> 테스트 방식에서 UnitTest는 문제가 될 만한 부분만 하고
-> 통합테스트로 기본적인 기능들을 테스트 하는방향으로
-> 에러 처리같은 것, 또 updateAt을 null로 처음에 초기화하는 것
-> Service에서 get 메서드 형태말고 find로 주고 null형태의 자료구조나 빈 String을 반환하는 것으로 가는게 맞을듯?