spring:
  application:
    name: discodeit

  profiles:
    active: dev     # 기본은 dev

springdoc:
  swagger-ui:
    enabled: true

logging:
  level:
    root: info
    org.hibernate.SQL: debug
    org.hibernate.type: trace
