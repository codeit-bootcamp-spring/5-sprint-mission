spring:
  profiles:
    active: dev   # 기본 실행 프로파일 (운영 배포 시 prod로 바꿔줌)

  application:
    name: discodeit

discodeit:
  storage:
    type: local
    local:
      root-path: ./data.dir
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs