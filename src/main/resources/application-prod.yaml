spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://prod-server:5432/discodeit
    username: prod_user
    password: prod_password
  jpa:
    hibernate:
      ddl-auto: validate