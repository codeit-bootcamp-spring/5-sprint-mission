server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://database-2.c5wgcwkau08s.ap-northeast-2.rds.amazonaws.com:5432/postgres
    username: user1
    password: yunji32274
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.sprint.mission.discodeit: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

management:
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true